import { Modal } from 'antd';
import  AppBase, {$api} from 'components/scripts/index';

export default class extends React.Component {
  render() {
    const { }
    return (
      <Modal className="exm-modal-user-add">
        USER_ADD + EXM
      </Modal>
    );
  }
}

